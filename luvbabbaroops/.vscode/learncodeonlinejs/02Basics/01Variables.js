    var fullName ="Aman Kumar";

    //how to name variable

    // fullName
    // full-name
    // full_name    *good convention while nameing the variables helps camel case is recommeded*
    // FullName

    var courseName ="React JS Bootcamp";

    var isLoggedIn = false;

    var LoggedCount = 34;

    // console.log(LoggedCount);
    // console.log("LoggedCount");  // *for the value and for the string at it is* .

    var paymentMode;

    paymentMode = "Credit Card";
    console.log(paymentMode);

    // var if = 23;   * if is a reserve keyword which we cannot use i.e we cannot put a value 23 using if keywor*